<Page x:Class="ControlStudy.StudentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ControlStudy"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StudentPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        
        <DataGrid ColumnWidth="*" Grid.Row="1" IsReadOnly="True" AutoGenerateColumns="False" Margin="10" x:Name="dataGridProgress" FontSize="15" Opacity="0.85" Background="Azure"  AlternatingRowBackground="#9ACeeb" AlternationCount="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дисциплина"      Binding="{Binding Discipline.Discipline1}" Width="1.5*"/>
                <DataGridTextColumn Header="Оценка"     Binding="{Binding Grade}" Width="*"/>
                <DataGridTextColumn Header="Дата"    Binding="{Binding DateGrade, StringFormat= dd.MM.yyyy}" Width="*"/>
            </DataGrid.Columns>
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#9ACeeb"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Height" Value="30"/>
                </Style>
            </DataGrid.Resources>
        </DataGrid>

        <Border Background="Black" Opacity="0.5"/>
        <Viewbox Stretch="Uniform" MinHeight="16" MaxWidth="850" Margin="8" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" Margin="0 16">
                <Canvas Margin="0 0 150 0">
                    <ComboBox x:Name="disciplineCB" SelectionChanged="DisciplineCBSelectionChanged"  FontSize="16" Width="200" SelectedItem="{Binding Discipline}"  DisplayMemberPath="Discipline1" />

                    <TextBlock IsHitTestVisible="False" FontFamily="Comic Sans MS" Text="Дисциплина" Foreground="DarkGray" FontSize="16">
                        <TextBlock.Style>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=disciplineCB}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Canvas>

                <Canvas Margin="100 0 150 0">
                    <ComboBox x:Name="semesterCB" SelectionChanged="SemesterCBSelectionChanged"  FontSize="16" Width="120" SelectedItem="{Binding Semester}">
                        <ComboBoxItem Content="1"/>
                        <ComboBoxItem Content="2"/>
                    </ComboBox>

                    <TextBlock IsHitTestVisible="False" FontFamily="Comic Sans MS" Text="Семестр" Foreground="DarkGray" FontSize="16">
                        <TextBlock.Style>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=semesterCB}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Canvas>
            </StackPanel>
        </Viewbox>

    </Grid>
</Page>
